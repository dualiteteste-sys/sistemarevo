import { BaseRepository } from './BaseRepository';
		import { Configuracao } from '../types';
		import { snakeToCamel } from '../lib/utils';
		export class ConfiguracoesRepository extends BaseRepository<Configuracao> {
		  constructor() {
		    super('configuracoes');
		  }
		  protected createEntity(data: any): Configuracao {
		    return data as Configuracao;
		  }
		  async getAllSettings(): Promise<Configuracao[]> {
		    console.warn('Backend desconectado. Retornando configurações vazias.');
		    return Promise.resolve([]); // Retorna array vazio para evitar chamadas ao backend
		  }
		  async saveSettings(settings: { chave: string; valor: any }[]): Promise<void> {
		    console.warn('Backend desconectado. Simulação de salvamento de configurações.');
		    console.log('Mock saveSettings:', settings);
		    return Promise.resolve(); // Não faz nada
		  }
		}
