import { BaseRepository } from './BaseRepository';
		import { Configuracao } from '../types';
		import { IConfiguracoesRepository } from './interfaces';
		export class ConfiguracoesRepository extends BaseRepository<Configuracao> implements IConfiguracoesRepository {
		  constructor() {
		    super('configuracoes');
		  }
		  protected createEntity(data: any): Configuracao {
		    return data as Configuracao;
		  }
		  async getAllSettings(): Promise<Configuracao[]> {
		    console.warn('[Modo de Construção] Chamada para getAllSettings bloqueada. Retornando configurações vazias.');
		    return Promise.resolve([]);
		  }
		  async saveSettings(settings: { chave: string; valor: any }[]): Promise<void> {
		    console.warn('[Modo de Construção] Chamada para saveSettings bloqueada.');
		    return Promise.resolve();
		  }
		}
